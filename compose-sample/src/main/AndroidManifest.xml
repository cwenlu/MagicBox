<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cwl.use_case">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MagicBox">
        <activity android:name=".compose.ComposeMainActivity" />
        <activity
            android:name=".hilt.HiltMainActivity"
            android:exported="false" />
        <activity
            android:name=".flow.with_lifecycle.CorrectUseFlowActivity"
            android:exported="false" />
        <activity
            android:name=".flow.FlowMainActivity"
            android:exported="false" />
        <activity
            android:name=".flow.with_lifecycle.LifecycleFlowActivity"
            android:exported="false" />

        <activity
            android:name="com.cwl.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MagicBox">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>